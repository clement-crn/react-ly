import pop from "./pop.mp3"
import money from "./money.mp3"
import start from "./start.mp3"
import dice from "./dice.mp3"
import soft from "./soft.mp3"

const sounds = {
  pop: new Audio(pop),
  money: new Audio(money),
  start: new Audio(start),
  dice: new Audio(dice),
  soft: new Audio(soft),
}

export default sounds
